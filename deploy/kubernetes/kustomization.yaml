apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-gateway-ui

# secret.example.yaml is intentionally NOT listed — copy it to secret.yaml (gitignored),
# fill in real values, and apply separately or via a secret manager.
resources:
  - namespace.yaml
  - configmap.yaml
  - networkpolicy.yaml
  - bff-deployment.yaml
  - web-deployment.yaml
  - service.yaml
  - ingress.yaml
